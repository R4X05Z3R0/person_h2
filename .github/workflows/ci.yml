name: ci.yml
on:
  push:
    branches:
      - [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Running Tests
        run: mvn test